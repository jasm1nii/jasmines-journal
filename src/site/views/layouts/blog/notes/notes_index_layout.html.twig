<!DOCTYPE html>
<html lang="en-US">
    <head>
        {{ include('@includes/head.shtml') }}
        <title>jasmine's notes</title>
        <meta name="description" content="microblog entries"/>
        <link rel="canonical" href="https://jasm1nii.xyz/blog/notes"/>
        <link id="base-theme" href="/_assets/stylesheets/blog_notes_index.css" rel="stylesheet" type="text/css" media="all"/>
        <noscript>
            <link href="/_assets/stylesheets/blog_notes_index_noscript.css" rel="stylesheet" type="text/css" media="all"/>
        </noscript>
    </head>
    <body>
        <div class="layout">
            <header>
                {# theme switch placeholder #}
                <hgroup>
                    <h1>jasmine's notes</h1>
                </hgroup>
                <nav>{{ nav|raw }}</nav>
            </header>
            <div class="main-column">
                <main>
                    <article>
                        {% if entries != null %}
                            {{ entries|raw }}
                        {% else %}
                            <p>that's too far!!!</p>
                        {% endif %}
                        <nav class="page-nav">
                            {% if page != 1 %}
                                <a class="previous" href="/blog/notes/page/{{ page - 1 }}">previous page</a>
                            {% endif %}
                            {% if page != total_pages + 1 %}
                                <a class="next" href="/blog/notes/page/{{ page + 1 }}">next page</a>
                            {% endif %}
                        </nav>
                    </article>
                    <aside class="feed-links">
                        <p><span aria-hidden="true">📰</span> subscribe via <a href="/notes.xml">atom</a> / <span aria-hidden="true">🐘</span> follow via <a href="https://fed.brid.gy/web/jasm1nii.xyz" rel="external me">fediverse</a></p>
                    </aside>
                </main>
                <footer>
                    {{ include('@includes/footer.shtml') }}
                </footer>
            </div>
        </div>
    </body>
</html>