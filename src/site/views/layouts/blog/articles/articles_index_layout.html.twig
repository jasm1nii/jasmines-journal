<!DOCTYPE html>
<html lang="en-US">
    <head>
        {{ include('@includes/head.shtml') }}
        <title>jasmine's b(rain)log</title>
        <meta name="description" content="my online diary"/>
        <link rel="canonical" href="https://jasm1nii.xyz/blog/articles"/>
        <link id="base-theme" href="/_assets/stylesheets/blog_articles_index.css" rel="stylesheet" type="text/css" media="all"/>
        <noscript>
            <link href="/_assets/stylesheets/blog_articles_index_noscript.css" rel="stylesheet" type="text/css" media="all"/>
        </noscript>
    </head>
    <body>
        <div class="layout">
            <header>
                {# theme switcher here #}
                <hgroup>
                    <h1>jasmine's b(rain)log</h1>
                </hgroup>
                <nav aria-label="primary">{{ nav|raw }}</nav>
            </header>
            <main>
                <article>
                    <h2>articles</h2>
                    {% if entries != null %}
                        <ol reversed class="articles-list">{{ entries|raw }}</ol>
                    {% else %}
                        <p>that's too far!!!</p>
                    {% endif %}
                </article>
                <nav class="page-nav">
                    {% if page != 1 %}
                        <a class="previous" href="/blog/articles/page/{{ page - 1 }}">previous page</a>
                    {% endif %}
                    {% if page != total_pages + 1 %}
                        <a class="next" href="/blog/articles/page/{{ page + 1 }}">next page</a>
                    {% endif %}
                </nav>
            </main>
        </div>
        <footer>
            {{ include('@includes/footer.shtml') }}
        </footer>
    </body>
</html>