<!DOCTYPE html>
<html lang="en-US">
    <head>
        {{ include('@includes/head.shtml') }}
        <title>jasmine's resources</title>
        <link rel="canonical" href="https://jasm1nii.xyz/resources"/>
        <meta name="description" content="a catalogue of helpful things for web design, art, and more!"/>
        <meta name="author" content="jasm1nii"/>
        <meta name="keywords" content="design resources, web development"/>
        <link id="base-theme" href="/_assets/stylesheets/resources.css" rel="stylesheet" type="text/css" media="all"/>
        <noscript>
            <link href="/_assets/stylesheets/resources_noscript.css" rel="stylesheet" type="text/css" media="all"/>
        </noscript>
    </head>
    <body>
        <header>
            <hgroup>
                <h1>jasmine's resources</h1>
                <p>
                    a list of links that i've personally found helpful and interesting!
                </p>
                <p>
                    last updated: <time datetime="{{ updated|date() }}">{{ updated|format_date(pattern="d MMMM yyyy")|lower }}</time>
                </p>
            </hgroup>
            <nav>
                {{ include('@includes/headernav.shtml') }}
            </nav>
        </header>
        <div id="left-col-group">
            <section id="guide">
                <nav>
                    {% apply markdown_to_html %}
                        {{ include('_toc.md') }}
                    {% endapply %}
                </nav>
            </section>
            <div id="center-col">
                <main>
                    <section id="legend">
                        {% apply markdown_to_html %}
                            {{ include('_legend.md') }}
                        {% endapply %}
                    </section>
                    <article>
                        {% apply markdown_to_html %}
                            {{ include('resources_index.md') }}
                        {% endapply %}
                    </article>
                </main>
                <footer>
                    {{ include('@includes/footer.shtml') }}
                </footer>
            </div>
        </div>
    </body>
</html>